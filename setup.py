from setuptools import setup

setup(
    name = 'parent',
    version = '1.0',
    description = 'Main folder for relative paths',
    author = 'Tije',
    author_email = '',
    packages = ['parent'],
    install_requires = ['wheel', 'bar', 'greek']
)